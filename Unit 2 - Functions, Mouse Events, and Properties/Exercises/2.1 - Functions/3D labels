Rect(0, 0, 400, 400, fill='aliceBlue')

def draw3DLabel(text):
    # Draw two labels, one centered slightly to the right of the other.
    ### Place Your Code Here ###
    Label(text, 205, 200, font='arial', size=65, bold=True, fill='darkGreen' )
    Label(text, 200, 200, font='arial', size=65, bold=True, fill='limeGreen' )
